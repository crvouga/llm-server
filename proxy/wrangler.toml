name = "llm-proxy"
main = "src/index.ts"
compatibility_date = "2025-06-03"

# Environment variables (set via Doppler in CI)
# Required secrets:
#   DATABASE_URL - PostgreSQL connection string (Supabase/PlanetScale HTTP API format)
#   BACKEND_URL - Backend service URL (default: https://lm-studio.chrisvouga.dev)

[env.production]
name = "llm-proxy-production"

[env.staging]
name = "llm-proxy-staging"
