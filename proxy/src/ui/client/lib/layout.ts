/** Full-width page content (dashboard, top bar inner). */
export const PAGE_CONTENT_CLASS = 'mx-auto w-full max-w-7xl min-w-0';

/** Narrow reading width for chat messages and input. */
export const PAGE_CONTENT_NARROW_CLASS = 'mx-auto w-full max-w-3xl min-w-0';

/** Horizontal padding shared by page body sections. */
export const PAGE_PADDING_CLASS = 'px-4 md:px-6';
