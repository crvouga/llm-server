{
  "name": "llm-proxy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "wrangler": "^3.82.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250603.1",
    "typescript": "^5.6.2"
  }
}
