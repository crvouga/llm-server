name: Deployment Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: deployment-pipeline-${{ github.ref }}
  cancel-in-progress: true

jobs:
  checks:
    name: Checks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        run: make install

      - name: Run Python tests
        run: make test

  deploy:
    name: Deploy
    needs: checks
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    env:
      DOPPLER_PROJECT: personal
      DOPPLER_CONFIG: dev
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Doppler CLI
        uses: dopplerhq/cli-action@v3

      - name: Set up Flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Install deployment utilities
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Deploy LiteLLM to Fly.io
        run: doppler run -- ./scripts/deploy-litellm.sh
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_SERVICE_TOKEN }}

      - name: Verify litellm.chrisvouga.dev
        run: |
          set -euo pipefail
          for attempt in $(seq 1 24); do
            if curl -fsS "https://litellm.chrisvouga.dev/health/liveliness" >/dev/null; then
              echo "https://litellm.chrisvouga.dev is healthy."
              exit 0
            fi
            echo "Attempt ${attempt}/24: endpoint not healthy yet, retrying in 5s..."
            sleep 5
          done
          echo "https://litellm.chrisvouga.dev did not become healthy during verification."
          exit 1
