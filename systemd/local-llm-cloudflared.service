[Unit]
Description=Template for Local LLM Cloudflare tunnel service
After=network.target

[Service]
Type=simple
ExecStart=cloudflared tunnel --config ~/.cloudflared/local-llm.yml run
Restart=always
RestartSec=5

[Install]
WantedBy=default.target

